/**
 * _template — KOPIEER deze map voor elke nieuwe calculator.
 *
 * Vul de 6 bestanden in, zoek naar "TODO" om de plekken te vinden
 * die je moet aanpassen. Daarna:
 *
 *   1. src/data/calculators.ts       → importeer META en voeg toe aan registry[]
 *   2. src/lib/calculators/component-registry.tsx → registry.set("...", () => import("./modules/[slug]/ui"))
 *   3. src/data/calculator-faqs.ts   → importeer FAQs en voeg toe aan explicitFaqs[]
 */
